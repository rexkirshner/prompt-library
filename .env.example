# Database
# PostgreSQL connection string from Vercel Postgres
# Format: postgresql://user:password@host:port/database?sslmode=require
DATABASE_URL="postgresql://placeholder"

# NextAuth.js
# The URL of your application (adjust port as needed for local development)
NEXTAUTH_URL=http://localhost:3001

# Secret used to encrypt JWT tokens and session cookies
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Note: This project uses email/password authentication (see DECISIONS.md D002)
# Google OAuth references removed - can be added back if OAuth support is implemented in future

# Google Analytics
# Optional: GA4 Measurement ID for analytics tracking
# Get this from Google Analytics 4 dashboard (format: G-XXXXXXXXXX)
# Leave empty to disable Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=
