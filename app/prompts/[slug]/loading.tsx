/**
 * Prompt Detail Page Loading State
 *
 * Displayed while a prompt detail page is loading.
 */

import { PromptDetailSkeleton } from '@/components/Skeleton'

export default function Loading() {
  return <PromptDetailSkeleton />
}
